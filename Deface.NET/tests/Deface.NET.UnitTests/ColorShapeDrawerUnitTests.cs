namespace Deface.NET.UnitTests;

public class ColorShapeDrawerUnitTests
{
    //[Fact]
    //public void Test()
    //{
    //    DetectedObject obj = new(10, 10, 40, 40, 1);
    //    DetectedObject obj2 = new(50, 50, 80, 80, 1);
    //    Settings settings = new(null);
    //    settings.AnonimizationShape = AnonimizationShape.Rectangle;

    //    int width = 400;
    //    int height = 400;

    //    using var bitmap = new SKBitmap(width, height);
    //    using var canvas = new SKCanvas(bitmap);

    //    var paint = new SKPaint
    //    {
    //        Color = SKColors.White
    //    };

    //    canvas.DrawRect(0, 0, width, height, paint);


    //    ColorShapeDrawer drawer = new();

    //    drawer.Draw(bitmap, [obj, obj2], settings);

    //    using var image = SKImage.FromBitmap(bitmap);
    //    using var data = image.Encode(SKEncodedImageFormat.Png, 100);
    //    using var stream = File.OpenWrite("white_bitmap.png");

    //    data.SaveTo(stream);
    //}
}
